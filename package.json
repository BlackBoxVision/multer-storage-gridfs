{
  "name": "multer-storage-gridfs",
  "version": "1.0.2",
  "description": "Multer Storage Engine to save uploads directly to MongoDB GridFS",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf ./dist",
    "compile": "babel ./src -d ./dist",
    "build": "npm run clean && npm run compile",
    "npm-publish": "npm run build && npm publish"
  },
  "repository": "git+https://github.com/BlackBoxVision/multer-gridfs.git",
  "keywords": [
    "multer",
    "storage",
    "mongo",
    "gridfs"
  ],
  "author": "BlackBox Vision",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/BlackBoxVision/multer-gridfs/issues"
  },
  "homepage": "https://github.com/BlackBoxVision/multer-gridfs#readme",
  "dependencies": {
    "gridfs-stream": "^1.1.1"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-preset-babili": "^0.0.10",
    "babel-preset-es2015": "^6.22.0",
    "babel-preset-stage-0": "^6.22.0",
    "mongoose": "^4.8.1",
    "rimraf": "^2.5.4"
  }
}
